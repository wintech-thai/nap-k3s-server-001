wordpress:
  enabled: true
  replicaCount: 1
  wordpressScheme: https
  wordpressUsername: admin
  existingSecret: 'wordpress-user-login'

  readinessProbe:
    enabled: false

  image:
    repository: bitnamilegacy/wordpress

  ingress:
    annotations:
      nginx.ingress.kubernetes.io/proxy-body-size: 50m
      kubernetes.io/ingress.allow-http: "true" #Wordpress will redirect to https internally

    ingressClassName: nginx # iap-nginx
    enabled: true

    hostname: siamgreenlegend.com
    extraHosts:
      - name: www.siamgreenlegend.com
      - name: sgl.wintech-thai.com

  service:
    type: ClusterIP

  mariadb:
    enabled: false

  externalDatabase:
    host: web-sgl-main-mysql
    port: 3306
    database: wordpress
    user: root
    existingSecret: wordpress-db-login

mysql:
  enabled: true
  image:
    repository: bitnamilegacy/mysql
    tag: 8.0.28
  auth:
    database: wordpress
    existingSecret: wordpress-db-login

#  primary:
#    command:
#      - "/opt/bitnami/mysql/bin/mysqld"
#      - "--defaults-file=/opt/bitnami/mysql/conf/my.cnf"
#      - "--basedir=/opt/bitnami/mysql"
#      - "--datadir=/bitnami/mysql/data"
#      - "--socket=/opt/bitnami/mysql/tmp/mysql.sock"
#      - "--pid-file=/opt/bitnami/mysql/tmp/mysqld.pid"
